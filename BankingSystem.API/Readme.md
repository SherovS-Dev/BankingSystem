# 🏦 Banking System API

Современная банковская система на базе ASP.NET Core Web API и PostgreSQL, разработанная для управления счетами, транзакциями и операциями клиентов.

## 📋 Содержание

- [О проекте](#о-проекте)
- [Технологии](#технологии)
- [Архитектура](#архитектура)
- [Функциональность](#функциональность)
- [Установка и запуск](#установка-и-запуск)
- [API Endpoints](#api-endpoints)
- [База данных](#база-данных)
- [Безопасность](#безопасность)
- [Структура проекта](#структура-проекта)

## 🎯 О проекте

Banking System API - это RESTful веб-сервис для управления банковскими операциями, включающий:

- Управление счетами клиентов
- Проведение транзакций (пополнение, снятие, переводы)
- История операций
- Система безопасности и аудита

Проект демонстрирует применение современных практик разработки на .NET, включая Repository Pattern, Dependency Injection, и работу с PostgreSQL.

## 🛠 Технологии

### Backend
- **ASP.NET Core 8.0** - Фреймворк для создания веб-API
- **C# 12** - Язык программирования
- **PostgreSQL 14+** - Реляционная база данных
- **Npgsql 8.x** - ADO.NET провайдер для PostgreSQL

### Паттерны и практики
- **Repository Pattern** - Абстракция доступа к данным
- **Dependency Injection** - Инверсия управления зависимостями
- **DTO (Data Transfer Objects)** - Разделение моделей данных
- **Stored Procedures** - Бизнес-логика на уровне БД

## 🏗 Архитектура

Проект построен с использованием многоуровневой архитектуры:

```
┌─────────────────────────────────────┐
│     Controllers (API Layer)         │
│  - AccountsController               │
│  - TransactionsController           │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Services (Business Logic)       │
│  - TransactionService               │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Repositories (Data Access)      │
│  - AccountRepository                │
│  - TransactionRepository            │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│         PostgreSQL Database         │
│  - Stored Procedures                │
│  - Triggers & Constraints           │
└─────────────────────────────────────┘
```

### Слои приложения

**1. Controllers (Контроллеры)**
- Обрабатывают HTTP-запросы
- Валидация входных данных
- Возвращают HTTP-ответы
- Логирование операций

**2. Services (Сервисы)**
- Содержат бизнес-логику
- Оркестрируют работу репозиториев
- Обработка транзакций
- Вызов хранимых процедур

**3. Repositories (Репозитории)**
- Прямая работа с базой данных
- CRUD операции
- Использование Npgsql для работы с PostgreSQL

**4. Database (База данных)**
- Хранимые процедуры для транзакций
- Триггеры и ограничения целостности
- Индексы для оптимизации запросов

## ✨ Функциональность

### Управление счетами
- ✅ Получение информации о счете
- ✅ Получение всех счетов клиента
- ✅ Проверка баланса счета

### Транзакции
- ✅ **Пополнение счета** - добавление средств
- ✅ **Снятие средств** - списание с проверкой баланса
- ✅ **Перевод между счетами** - атомарная операция
- ✅ **История транзакций** - с пагинацией

### Дополнительно
- 📝 Детальное логирование всех операций
- 🔒 Валидация данных на всех уровнях
- 🎯 Уникальные референс-номера для транзакций
- 💾 Сохранение баланса после каждой операции

## 🚀 Установка и запуск

### Требования
- .NET SDK 8.0 или выше
- PostgreSQL 14 или выше
- IDE (Visual Studio, Rider, VS Code)

### Шаги установки

1. **Клонирование репозитория**
```bash
git clone https://github.com/your-username/banking-system-api.git
cd banking-system-api
```

2. **Настройка базы данных**

Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE BankingSystemDB;
```

Выполните SQL-скрипт для создания схемы:
```bash
psql -U postgres -d BankingSystemDB -f Database/01_CreateSchema.sql
```

3. **Настройка строки подключения**

Отредактируйте `appsettings.json`:
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=BankingSystemDB;Username=postgres;Password=your_password"
  }
}
```

4. **Запуск приложения**
```bash
dotnet restore
dotnet build
dotnet run
```

API будет доступен по адресу: `https://localhost:5001` или `http://localhost:5000`

## 📡 API Endpoints

### Accounts (Счета)

#### Получить информацию о счете
```http
GET /api/accounts/{id}
```

**Ответ:**
```json
{
  "accountId": 1,
  "customerId": 1,
  "accountNumber": "TJ72001234567890123456",
  "accountType": "Checking",
  "balance": 15000.00,
  "currency": "TJS",
  "status": "Active"
}
```

#### Получить все счета клиента
```http
GET /api/accounts/customer/{customerId}
```

#### Получить баланс счета
```http
GET /api/accounts/{id}/balance
```

### Transactions (Транзакции)

#### Получить информацию о транзакции
```http
GET /api/transactions/{id}
```

#### Получить историю транзакций
```http
GET /api/transactions/account/{accountId}?page=1&pageSize=20
```

#### Пополнение счета
```http
POST /api/transactions/deposit
Content-Type: application/json

{
  "accountId": 1,
  "amount": 5000.00,
  "description": "Пополнение счета"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Deposit completed successfully",
  "transactionId": 15,
  "newBalance": 20000.00
}
```

#### Снятие средств
```http
POST /api/transactions/withdraw
Content-Type: application/json

{
  "accountId": 1,
  "amount": 2000.00,
  "description": "Снятие наличных"
}
```

#### Перевод между счетами
```http
POST /api/transactions/transfer
Content-Type: application/json

{
  "fromAccountId": 1,
  "toAccountId": 2,
  "amount": 1000.00,
  "description": "Перевод другу"
}
```

## 🗄 База данных

### Основные таблицы

**Customers** - Информация о клиентах
- CustomerId (PK)
- FirstName, LastName, MiddleName
- Email, PhoneNumber, PassportNumber
- Address, City, Country
- DateOfBirth, IsActive

**Accounts** - Банковские счета
- AccountId (PK)
- CustomerId (FK)
- AccountNumber (unique)
- AccountType (Checking/Savings/Credit)
- Balance, Currency, Status

**Transactions** - История операций
- TransactionId (PK)
- AccountId (FK)
- TransactionType (Deposit/Withdrawal/Transfer)
- Amount, Currency, Status
- ReferenceNumber, BalanceAfter

### Хранимые процедуры

**sp_deposit_funds** - Пополнение счета
```sql
SELECT * FROM sp_deposit_funds(
  p_account_id := 1,
  p_amount := 5000.00,
  p_description := 'Пополнение',
  p_created_by := 'System'
);
```

**sp_withdraw_funds** - Снятие средств
```sql
SELECT * FROM sp_withdraw_funds(
  p_account_id := 1,
  p_amount := 2000.00,
  p_description := 'Снятие',
  p_created_by := 'System'
);
```

**sp_transfer_funds** - Перевод между счетами
```sql
SELECT * FROM sp_transfer_funds(
  p_from_account_id := 1,
  p_to_account_id := 2,
  p_amount := 1000.00,
  p_description := 'Перевод',
  p_created_by := 'System'
);
```

### Особенности реализации

✅ **Атомарность транзакций** - все операции выполняются внутри транзакций
✅ **Проверка баланса** - предотвращение овердрафта
✅ **Уникальные референсы** - каждая транзакция имеет уникальный номер
✅ **Сохранение истории** - баланс после каждой операции
✅ **Индексы** - оптимизация запросов по ключевым полям

## 🔒 Безопасность

### Реализованные меры

- ✅ Валидация входных данных (Data Annotations)
- ✅ Проверка существования счетов
- ✅ Проверка достаточности средств
- ✅ Ограничения на суммы транзакций:
    - Депозит: 0.01 - 10,000,000 TJS
    - Снятие: 0.01 - 50,000 TJS
    - Перевод: 0.01 - 1,000,000 TJS
- ✅ Логирование всех операций
- ✅ Обработка исключений с безопасными сообщениями

### Будущие улучшения

- 🔄 JWT аутентификация
- 🔄 Авторизация на основе ролей
- 🔄 Rate limiting
- 🔄 Шифрование чувствительных данных
- 🔄 Two-factor authentication

## 📁 Структура проекта

```
BankingSystem.API/
│
├── Controllers/              # API контроллеры
│   ├── AccountsController.cs
│   └── TransactionsController.cs
│
├── Services/                 # Бизнес-логика
│   └── TransactionService.cs
│
├── Repositories/             # Доступ к данным
│   ├── AccountRepository.cs
│   └── TransactionRepository.cs
│
├── Interfaces/               # Интерфейсы
│   ├── IAccountRepository.cs
│   ├── ITransactionRepository.cs
│   └── ITransactionService.cs
│
├── Models/                   # Модели данных
│   ├── Account.cs
│   ├── Customer.cs
│   ├── Transaction.cs
│   └── User.cs
│
├── DTOs/                     # Data Transfer Objects
│   ├── DepositRequest.cs
│   ├── WithdrawalRequest.cs
│   ├── TransferRequest.cs
│   └── TransactionResultDto.cs
│
├── Database/                 # SQL скрипты
│   └── 01_CreateSchema.sql
│
└── Program.cs               # Точка входа приложения
```

## 🧪 Тестирование

### Примеры тестовых данных

В базе данных созданы тестовые счета:

```
Клиент 1: Фаррух Исмоилов
  - Счет 1: TJ72001234567890123456 (Checking, 15000 TJS)
  - Счет 2: TJ72001234567890123457 (Savings, 50000 TJS)

Клиент 2: Нигина Шарипова
  - Счет 3: TJ72001234567890123458 (Checking, 8000 TJS)

Клиент 3: Джамшед Назаров
  - Счет 4: TJ72001234567890123459 (Savings, 120000 TJS)
```

### Тестовые сценарии

**Сценарий 1: Пополнение счета**
```bash
curl -X POST https://localhost:5001/api/transactions/deposit \
  -H "Content-Type: application/json" \
  -d '{
    "accountId": 1,
    "amount": 5000,
    "description": "Тестовое пополнение"
  }'
```

**Сценарий 2: Перевод между счетами**
```bash
curl -X POST https://localhost:5001/api/transactions/transfer \
  -H "Content-Type: application/json" \
  -d '{
    "fromAccountId": 1,
    "toAccountId": 2,
    "amount": 1000,
    "description": "Тестовый перевод"
  }'
```

## 📊 Производительность

### Оптимизации

- 🚀 Индексы на часто используемых полях
- 🚀 Использование хранимых процедур
- 🚀 Пагинация для больших списков
- 🚀 Асинхронные операции (async/await)
- 🚀 Connection pooling в Npgsql

### Рекомендации для production

- Настройка connection pool
- Кэширование справочных данных
- Мониторинг производительности
- Настройка индексов PostgreSQL
- Регулярное обслуживание БД (VACUUM, ANALYZE)

## 🔄 Roadmap

### Версия 2.0
- [ ] JWT аутентификация и авторизация
- [ ] Swagger/OpenAPI документация
- [ ] Unit и Integration тесты
- [ ] Docker контейнеризация
- [ ] CI/CD pipeline

### Версия 3.0
- [ ] Микросервисная архитектура
- [ ] Message queue (RabbitMQ/Kafka)
- [ ] Распределенные транзакции
- [ ] GraphQL endpoint
- [ ] Real-time уведомления (SignalR)

## 👥 Автор

Разработано для демонстрации навыков работы с:
- ASP.NET Core Web API
- PostgreSQL и SQL
- Repository Pattern
- RESTful API design
- Банковских систем

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Контакты

Если у вас есть вопросы или предложения, свяжитесь со мной:
- Email: your.email@example.com
- LinkedIn: your-linkedin-profile
- GitHub: your-github-username

---

**Примечание**: Этот проект является учебным и не предназначен для использования в продакшене без дополнительных мер безопасности и тестирования.